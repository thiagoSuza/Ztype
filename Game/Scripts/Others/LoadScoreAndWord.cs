using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LoadScoreAndWord : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
       /* GameController.instance.wordNumber = PlayerPrefs.GetInt("WordsToSave");
        HudController.instance.intScore = PlayerPrefs.GetInt("ScoreToSave");*/
    }

    
}
